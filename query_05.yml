label:
  Which historic buildings are located in Copenhagen?
category:
  buildm
sparql: >
  PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
  SELECT ?histBuilding, ?year FROM  <http://data.duraark.eu/sdas>
  WHERE {
    ?histBuilding <http://data.duraark.eu/vocab/buildm/completionDate> ?year .
    FILTER (xsd:integer(?year)<1950)
  }
