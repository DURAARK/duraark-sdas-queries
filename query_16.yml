label:
  Give me a list of famous churches in Denmark.
category:
  contextual
sparql: >
  SELECT ?building FROM <http://data.duraark.eu/crawl/3> WHERE{
    ?building ?type ?value.
    FILTER regex(?value,"Church").
  }
  LIMIT 40
