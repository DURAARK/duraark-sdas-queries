label:
  Give me a list of famous churches all over the world.
category:
  contextual
show:
  false
urlField:
  building
sparql: >
  SELECT ?building FROM <http://data.duraark.eu/crawl/3> WHERE{
    FILTER regex(?value,"Church") .
  }
  LIMIT 40
